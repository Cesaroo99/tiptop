# Production — secrets uniquement dans le dashboard Render, pas dans git.
NODE_ENV=production
WEB_ORIGIN=https://tiptop-phone.onrender.com
DATABASE_URL=from-render
SESSION_SECRET=from-render
OTP_ALLOW_MOCK=1
PAYMENT_WEBHOOK_SECRET=REPLACE
# OPENAI_API_KEY=
NEXT_PUBLIC_API_URL=
API_INTERNAL_URL=http://127.0.0.1:3001
# admin.tiptop.ca n’est pas encore créé — voir docs/admin/DEPLOYMENT.md
